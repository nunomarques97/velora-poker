{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window and the HUD overlay window",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "opener:default",
    "dialog:default"
  ]
}
